#include <stdio.h>

int main() {
    int a, b, sum;
    printf("enter two numbers to add");
    scanf("%d  %d", &a, &b);
    sum=a+b;
    printf("The sum of %d and %d is %d",a,b, sum);
    return 0;
}
